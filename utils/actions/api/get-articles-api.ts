"use server";

export const getAllArticlesApi = async (userId: string) => {
  try {
    // TODO: Replace with Convex query
    return [] as unknown[];
  } catch (error: unknown) {
    return error;
  }
};
